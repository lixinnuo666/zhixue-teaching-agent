# 作品 Demo 视频脚本（3 分钟）

> 用途：粤港澳大湾区 AI Coding 创新大赛，「多模态教学智能体」作品演示视频
> 形式建议：1920×1080 录屏 + 画外音解说，语速约 180 字/分钟，全片约 500 字解说词
> 录制前准备：浏览器全屏、关闭通知、准备一张含文字的讲义截图（手册里那张生物·光合作用讲义即可）

---

## 0:00 – 0:18 ｜ 开场：一句话说清是什么

| 项 | 内容 |
| --- | --- |
| 画面 | 应用首页，鼠标缓慢划过七个功能标签 |
| 操作 | 无，纯展示 |
| 解说 | 「老师手里有一沓讲义截图，学生有一堆问不完的问题。我们做的『智学』，把讲义丢进去，就能自动拆出知识点、画成图谱，还能当场答疑、出题、出学习报告——全部在浏览器里完成，不需要服务器。」 |

**要点**：前 15 秒必须让评委听懂"它是干什么的"。不要先讲技术。

---

## 0:18 – 0:52 ｜ 核心亮点一：图文解析

| 项 | 内容 |
| --- | --- |
| 画面 | 左侧素材库 → 拖拽图片进虚线框 → 右侧预览区出现图像 |
| 操作 | 1）拖入图片 2）点「▶ 识别图中文字」3）点「文字区标注」 |
| 解说 | 「第一步是图文解析。拖进一张讲义截图，系统会先用 Canvas 做视觉分析：算出尺寸、主色分布、亮度直方图，判断清晰度和是否倾斜，还能自动框出文字区域——这些全部在本地完成，图片不会上传到任何服务器。点识别，Tesseract 引擎把图里的文字抽出来，右边可以直接编辑修正。」 |

**要点**：强调"本地处理、不上传"——这是隐私卖点，评委在意。

---

## 0:52 – 1:32 ｜ 核心亮点二：知识结构化

| 项 | 内容 |
| --- | --- |
| 画面 | 切到「② 知识结构」→ 思维导图展开 → 切到「③ 知识图谱」拖拽节点 |
| 操作 | 1）点第二个标签，点「展开全部」2）拖动粒度滑块 3）切第三个标签，拖一个节点、滚轮缩放 |
| 解说 | 「抽出来的文字会被切成知识点。系统识别哪些是核心概念、哪些是事实数据、哪些是公式符号，再算出它们之间的依赖关系。左边是思维导图，可以按章节、按类型、按重要度重新分区，粒度滑块能控制展开到第几层。右边是知识图谱，节点按学科聚拢，可以拖拽钉住、滚轮缩放，点图例只看某一类。」 |

**要点**：这一段展示"结构化"能力，是赛题要求的「知识点结构化抽取」，要给足时长。

---

## 1:32 – 2:05 ｜ 核心亮点三：答疑辅导

| 项 | 内容 |
| --- | --- |
| 画面 | 切到「④ 答疑辅导」→ 输入问题 → 回答右上角出现蓝色「大模型生成」标签 → 切换讲解方式 → 对比回答 |
| 操作 | 1）点第四个标签 2）输入「什么是光合作用？它分几个阶段？」3）**特写右上角蓝色「大模型生成」标签** 4）依次点「分步讲解」「举例说明」「引导启发」对比回答 |
| 解说 | 「有了知识点就能答疑。这里用的是 RAG 架构：系统先在本地语料里检索出最相关的原文片段作为事实依据，再交给大模型来组织表达——注意答案右上角这个蓝色标签，说明这次是由大模型生成的。这么做的好处是，答案既用到了你自己的材料，不会凭空编造，又比纯关键词检索更连贯、更像老师在讲。最有意思的是四种讲解方式：同一个问题，可以要直接回答、要分步讲解、要举例子，或者选『引导启发』——它不直接给答案，而是反过来提问，引导学生自己想出来。这个对教学场景很实用。」 |

**要点**：
- 四种讲解方式是差异化亮点，一定要都点到一下。
- **必须拍到蓝色「大模型生成」标签**，这是"确实用了大模型"的视觉证据，评委就认这个。
- 建议再问一个需要推理的问题（比如"这两个概念有什么区别？"），展示比关键词检索更聪明的回答，对比效果更强烈。
- 配置 Key 的动作**不要放进录屏**（输入框虽是密码框，但仍建议录屏开始前就配好）。

---

## 2:05 – 2:35 ｜ 核心亮点四：练习与报告

| 项 | 内容 |
| --- | --- |
| 画面 | 切到「⑤ 练习测评」→ 生成试卷 → 答题 → 提交判分 → 切「⑥ 学习报告」看雷达图 |
| 操作 | 1）点「生成试卷」2）随便答几题 3）点「提交并判分」4）切第六个标签 |
| 解说 | 「还能自动出题。系统根据知识点生成选择题，答完立刻判分，错题进错题本。学习报告页用雷达图展示各知识点的掌握度——哪些会了、哪些还不会，一眼看出来，方便针对性复习。」 |

**要点**：形成"学—练—评"闭环，呼应完整产品感。

---

## 2:35 – 2:52 ｜ 技术方案

| 项 | 内容 |
| --- | --- |
| 画面 | 切到编辑器/源码界面，快速划过目录结构；**重点停留 2 秒在 `assets/js/nlp/llm.js`**；或展示一张架构图 |
| 操作 | 打开 VS Code 展示 assets/js 目录 → 打开 nlp/llm.js |
| 解说 | 「技术上，整个项目是零依赖的原生 JavaScript，没有构建步骤。图像处理、知识图谱、思维导图全部基于 Canvas 手绘。OCR 用 Tesseract.js，联网下载一次中文模型后就能离线用。答疑环节我们接入了大模型 API——走的是 OpenAI 兼容协议，所以 DeepSeek、智谱、通义、Moonshot 可以任意切换。密钥只存在使用者自己的浏览器本地，不进代码、不上传服务器。**这里有个关键设计：大模型不可用时，系统会自动降级回本地规则引擎**，核心功能零中断——这在真实教学场景里很重要，不能因为网络波动就整个用不了。数据只存在浏览器内存，刷新即清空，不涉及任何隐私上传。」 |

**要点**：
- 突出"零依赖、零后端、纯前端"——既是工程完整性，也是部署成本低。
- **"自动降级"这句一定要说**，这是架构韧性，评委很吃这一套。
- 密钥本地存储这点顺带提一句，回应隐私与安全的评审关注点。

---

## 2:52 – 3:00 ｜ 收尾

| 项 | 内容 |
| --- | --- |
| 画面 | 回到首页，画面定格；出现作品名 + 团队 + 赛事名 |
| 操作 | 无 |
| 解说 | 「智学·多模态教学智能体，让每一份讲义都能变成可交互的知识网络。谢谢！」 |

---

## 录制检查清单

- [ ] 全程浏览器全屏、缩放 100%，保证文字可读
- [ ] 关闭弹窗通知、隐藏书签栏
- [ ] 提前跑一遍流程，避免现场 OCR 卡顿（首次识别要下载模型，建议先跑一次再开录）
- [ ] **开录前先配好大模型 Key 并测试连接成功**（配置过程不要入镜）
- [ ] 确认答疑答案右上角出现蓝色「大模型生成」标签
- [ ] 提前想好一个"需要推理"的问题备用，体现大模型比关键词检索聪明
- [ ] 解说录音与画面分开录，后期合成，避免口误重来
- [ ] 输出 1080p MP4，文件不超过赛事平台限制
- [ ] **视频必须展示真实操作**，不能只放静态截图——评委看"功能可用性"
- [ ] 全程检查画面里没有出现 API Key（包括设置面板、终端窗口、浏览器 DevTools）
