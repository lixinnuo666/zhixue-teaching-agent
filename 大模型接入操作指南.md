# 智学 · 接入大模型操作指南（方案 A：自带 Key）

> 全程大约 15 分钟，不需要改任何代码，不需要后端，不需要重新部署。
>
> **核心原则**：Key 只存在你自己浏览器的 localStorage 里，不进代码、不进 Git、不上传服务器。没填 Key 时应用照常跑（本地规则引擎），填了才走大模型。

---

## 第 0 步：先搞清楚会发生什么

| 状态 | 答疑页表现 | 说明 |
|---|---|---|
| 没配置 Key | 答案右上角显示灰色「本地引擎」 | 现状，TF-IDF + 余弦相似度检索，可用但不是大模型 |
| 配好 Key | 显示蓝色「大模型生成」 | 本地检索找素材（RAG 的 R），大模型组织表达（RAG 的 G） |
| Key 填了但调用失败 | 自动降级为「本地引擎」+ 黄色警告条 | **不会白屏、不会卡死**，评委看不出崩了 |

这个"失败自动降级"是你今天做的最重要的一件事——**比赛现场网络抽风也不会翻车**。

---

## 第 1 步：申请一个 API Key（5 分钟）

推荐顺序从上往下，挑一个就行：

| 服务商 | 申请地址 | 推荐模型 | 成本 | 特点 |
|---|---|---|---|---|
| **硅基流动**（首选） | https://cloud.siliconflow.cn | `Qwen/Qwen2.5-7B-Instruct` | **有免费额度** + 注册认证送 ¥16 | **✅ 实测浏览器可直连，无需代理**，国产、跑通最快 |
| DeepSeek | https://platform.deepseek.com | `deepseek-chat` | 约 ¥1/百万 token，充 ¥10 用到毕业 | 中文强、便宜（硅基流动平台上也有，不用另注册） |
| 智谱 GLM | https://open.bigmodel.cn | `glm-4-flash` | 有免费额度 | 国产大厂 |
| 通义千问 | 阿里云百炼控制台 | `qwen-plus` | 有免费额度 | 阿里云生态 |
| Moonshot | https://platform.moonshot.cn | `moonshot-v1-8k` | 有免费额度 | 长文本 |

**具体动作**：
1. 打开上表任一申请地址 → 手机号注册/登录
2. 找到「API Keys」或「密钥管理」页面
3. 点「创建新的 API Key」
4. **立刻复制保存**（很多平台只显示一次，关掉就没了）。Key 长这样：`sk-xxxxxxxxxxxxxxxxxxxxxxxx`
5. 如需充值，充 ¥10 足够整个赛程

> ⚠️ 这步别在直播间/录屏里做，Key 会露出。

---

## 第 2 步：启动应用（1 分钟）

### 方式 A：推荐 —— 用本地 HTTP 服务打开源码版

在项目目录打开终端，执行：

```bash
python -m http.server 8124
```

浏览器访问 `http://127.0.0.1:8124`

（没有 Python 也可以用：`npx serve -l 8124`）

### 方式 B：双击单文件版

直接双击 `智学-教学智能体-单文件版.html`

> ⚠️ **注意**：双击打开时页面地址是 `file://`，浏览器认为来源是 `null`，**大部分大模型 API 会拒绝这种来源**。如果发现连不上，回到方式 A，或用第 6 步的本地代理兜底。

---

## 第 3 步：打开大模型设置面板

页面顶栏右侧，找到新加的按钮：

```
⚙ 大模型          ← 点它
```

（位置在「本地引擎就绪」这个状态标签旁边）

点开后是这个弹窗：

```
┌─────────────────────────────────────────┐
│  大模型设置                          ×  │
├─────────────────────────────────────────┤
│  服务商   [ DeepSeek            ▾ ]     │
│  Base URL [ https://api.deepseek.com/v1]│
│  API Key  [ •••••••••••• ]  (密码框)    │
│  模型名   [ deepseek-chat ]             │
│  ☑ 启用大模型增强                       │
│  [ 测试连接 ]  状态文字显示在这里        │
│  [清除配置]        [取消]  [保存]        │
└─────────────────────────────────────────┘
```

**关键便利点**：选了服务商之后，Base URL 和模型名会**自动填好**，你只需要粘贴 Key。

---

## 第 4 步：填配置

1. **服务商**：选你刚才注册的那家（比如 DeepSeek）
2. **Base URL**：自动填好了，不用动
3. **API Key**：粘贴第 1 步复制的 `sk-...`
4. **模型名**：自动填好了，不用动
5. **☑ 启用大模型增强**：保持勾选

---

## 第 5 步：点「测试连接」

点按钮，等 1-3 秒，看下方状态文字：

| 显示 | 含义 | 怎么办 |
|---|---|---|
| 🟢 `连接成功（xxx ms）：正常` | 通了！ | 直接进第 6 步 |
| 🔴 `失败：鉴权失败（401/403）…` | Key 错了或没激活 | 重新复制 Key，确认没多粘空格 |
| 🔴 `失败：触发限流（429）…` | 余额不足或请求太密 | 充值 / 等一会再试 |
| 🔴 `失败：接口不存在（404）…` | Base URL 或模型名不对 | 核对模型名大小写 |
| 🔴 `失败：请求被浏览器拦截或网络不可达…` | **CORS 跨域拦了** | → 看第 7 步（兜底方案） |
| 🔴 `失败：请求超时…` | 网络慢 | 再点一次，或换家服务商 |

---

## 第 6 步：保存 + 验收

1. 点「**保存**」→ 提示「大模型配置已保存到本地」
2. 顶栏状态标签会从「本地引擎就绪」变成「**大模型就绪 · deepseek-chat**」
3. 切到「**答疑辅导**」标签页
4. 先确保已经上传过学习材料并完成「知识点抽取」（否则没有素材片段）
5. 随便问一个问题，比如"这个概念怎么理解？"

**验收标准**：答案右上角出现蓝色标签「**大模型生成**」，且答案明显更连贯、更像人话。

再试试右上角的四种讲解方式：
- **直接回答**：先给结论
- **分步讲解**：1. 2. 3. 编号
- **举例说明**：必带生活例子
- **启发引导**：不直接给答案，反问引导（苏格拉底式）

四种方式都会透传给大模型，这是很好的演示点。

---

## 第 7 步（备用）：万一被 CORS 拦了

> **实测结论（2026-09-24）：硅基流动 SiliconFlow 允许浏览器直连，不需要代理。**
> 如果你用的是硅基流动，这一整步基本可以跳过，留着给其他服务商兜底即可。

如果测试连接提示"请求被浏览器拦截"，说明这家 API 不允许浏览器直连。别慌，我们有本地代理。

**操作**：

1. 新开一个终端，进入项目目录，执行：

```bash
node tools/llm-proxy.mjs
```

要转发到别的家，先设环境变量：

```powershell
# PowerShell
$env:UPSTREAM="https://api.deepseek.com/v1"
node tools/llm-proxy.mjs
```

2. 看到这样的输出说明代理起来了：

```
=========================================
  智学 · 大模型本地代理已启动
  监听:  http://127.0.0.1:8787/v1
  上游:  https://api.deepseek.com/v1
=========================================
```

3. 回到应用的大模型设置：
   - 服务商选「**本地代理（解决跨域）**」（Base URL 会自动填成 `http://127.0.0.1:8787/v1`）
   - API Key **照填你的真实 Key**
   - 模型名按上游填（DeepSeek 就是 `deepseek-chat`）
4. 再点「测试连接」→ 应该就通了

**这个代理只监听 127.0.0.1，外网访问不到，零依赖，关掉窗口即停止。**

---

## 第 8 步：队友怎么用

每个队友在**自己电脑**上重复第 1~6 步即可。

- Key 存在各自浏览器里，互不干扰
- 仓库里**没有任何 Key**，拉代码的人必须自己填
- 不想用大模型的队友，不填就行，应用照常跑本地引擎

想清除配置：设置面板里点「清除配置」，或浏览器 DevTools → Application → Local Storage → 删掉 `zhixue.llm.config`。

---

## 演示/答辩注意事项

1. **录 Demo 视频前**，确认浏览器没把 Key 框显示出来（输入框是 `type=password`，但点开设置面板时**先退出录屏再配置**，或者配置好再开始录）
2. **答辩现场**如果断网，应用自动降级为本地引擎，你要心里有数——提前准备一句解说："我们的架构是本地优先，大模型是增强层，网络不可用时自动降级，保证核心功能不中断。" 这反而是加分项，说明架构有韧性。
3. **别把 Key 写进任何文件**（包括 README、PPT、视频脚本）

---

## 常见问题

**Q：填了 Key 会上传到你们服务器吗？**
A：不会。应用是纯前端，请求从你的浏览器直连大模型厂商（或经本机代理），不经过任何中间服务器。

**Q：Key 会被提交到 GitHub 吗？**
A：不会。Key 只存在 localStorage，代码里没有硬编码任何密钥。

**Q：不填 Key 能用吗？**
A：能，完全能用，就是答案质量差一些。

**Q：调用要花多少钱？**
A：一次答疑约 500-1500 token。DeepSeek 大概 **一分钱能问 10-20 个问题**，整个比赛花不了几块钱。

**Q：换了电脑配置还在吗？**
A：不在。localStorage 是按浏览器+域名隔离的，换机器要重新填。

---

## 技术说明（写进 PPT 用这段）

> 采用 RAG（检索增强生成）架构：**本地检索引擎负责"找得准"（Retrieval）**，从用户上传的材料中用 TF-IDF + 余弦相似度召回最相关的原文片段作为事实依据；**大模型负责"讲得好"（Generation）**，在给定素材与讲解风格约束下组织答案，并强制声明"素材中未提及"以避免幻觉。
>
> 配置层支持 OpenAI 兼容协议，可切换 DeepSeek / 智谱 / 通义 / Moonshot / 硅基流动等任意兼容服务；密钥仅存于浏览器本地存储，不上传、不入库。
>
> **容错设计**：大模型不可用时自动降级为本地规则引擎，保证核心功能零中断。
